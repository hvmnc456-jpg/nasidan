# 나.시.단 UI 디자인 문서

## 1. 앱 개요

- **앱명**: 나.시.단 (fitness-tracker)
- **플랫폼**: PWA (모바일 최적화, max-width 430px)
- **테마**: 다크 (`bg-zinc-950`) + 라임 그린 포인트 (`lime-400`)
- **레이아웃 구조**: 하단 탭 네비게이션 고정 + 위 콘텐츠 영역 스크롤

---

## 2. 탭 구조

하단에 3개 탭이 항상 표시됨. 타이머 진행 중에는 탭 전환 불가 (비활성 탭 40% 투명도).

| 탭 ID | 라벨 | 아이콘 | 역할 |
|---|---|---|---|
| `home` | 홈 | 집 아이콘 | 운동 캘린더 + 누적 볼륨 통계 |
| `workout` | 운동 | 덤벨 아이콘 | 운동 플로우 전체 (부위선택 → 운동등록 → 타이머 → 요약) |
| `diet` | 식단 | 머그컵 아이콘 | 식단 기록 (미구현, 플레이스홀더) |

---

## 3. 화면 목록

### 3-1. 홈 탭 (`HomeTab`)

**역할**: 월별 운동 캘린더와 전체 누적 볼륨을 확인하는 대시보드

**UI 요소**:

| 영역 | 요소 | 설명 |
|---|---|---|
| 날짜 헤더 | "오늘" 레이블 + 날짜 (`YYYY년 M월 D일 (요일)` 형식) | 항상 오늘 날짜 표시 |
| 캘린더 | 월 네비게이션 (`←` / `→`) + 요일 헤더 + 날짜 그리드 | 월 이동 가능 |
| 캘린더 날짜 셀 | 오늘: 라임 원형 배경 / 운동한 날: 라임 점 + 흰 텍스트 / 미운동: 회색 텍스트 | 운동 기록 있는 날만 탭 가능 |
| 월 운동 횟수 뱃지 | `N회` 라임 pill — 해당 월 운동 횟수 | 0회면 미표시 |
| 날짜 상세 드로워 | 선택한 날짜 탭 시 캘린더 하단에 펼침 표시 | 해당 날 세션 목록 |
| 날짜 상세 > 세션 | 부위 뱃지 + 총 시간 + 운동 목록 (이름, 무게kg, 세트×횟수) + 간단 통계 | 삭제 버튼 포함 |
| 구분선 | | |
| 누적 kg 보드 | "누적 kg" 레이블 + 전체 합계 + 부위별 3열 그리드 | |
| 누적 보드 > 전체 합계 | `NNNkg` 큰 폰트 + "전체 누적 볼륨" 부제 | 무게 × 세트 × 횟수 합산 |
| 누적 보드 > 부위 카드 | 가슴 / 어깨 / 등 / 이두 / 삼두 / 하체 (6개, 복근 제외) | 값 있으면 흰색, 없으면 어두운 회색 |
| 빈 상태 | "운동을 완료하면 볼륨이 쌓여요" 안내 | 기록 없을 때만 표시 |

**사용자 행동**:
- 캘린더 `←` / `→` 탭 → 이전/다음 달 이동 (선택 날짜 초기화)
- 운동 기록 있는 날짜 탭 → 해당 날 세션 상세 펼침 (토글, 다시 탭하면 닫힘)
- 세션 삭제 아이콘 탭 → "취소" / "삭제" 확인 UI로 교체 (2단계 삭제)
- "삭제" 확인 탭 → 세션 삭제 + 날짜 선택 초기화

---

### 3-2. 운동 탭 — 부위 선택 화면 (`HomeScreen`)

**역할**: 오늘 운동할 신체 부위를 선택하는 진입 화면

**UI 요소**:

| 영역 | 요소 | 설명 |
|---|---|---|
| 헤더 | "운동 시작" 소제목 + "오늘 운동할 부위 선택" 타이틀 | |
| 부위 선택 그리드 | 3열 그리드, 각 버튼 h-16 | 가슴 / 어깨 / 등 / 이두 / 삼두 / 복근 / 하체 (7개) |
| 부위 버튼 (비선택) | `bg-zinc-900` 어두운 배경 + 회색 텍스트 | |
| 부위 버튼 (선택됨) | `bg-lime-400` 라임 배경 + 어두운 텍스트 + 그림자 | 탭 시 토글 |
| 선택 표시 텍스트 | "가슴 · 어깨 선택됨" — 선택된 부위 나열 | 1개 이상 선택 시 그리드 하단에 표시 |
| 다음 버튼 | `w-full h-14` 하단 고정 | 선택 없으면 비활성 (회색) / 선택 있으면 라임 |

**사용자 행동**:
- 부위 버튼 탭 → 선택/해제 토글 (복수 선택 가능)
- "다음 →" 탭 (1개 이상 선택 시) → 운동 목록 화면으로 이동

---

### 3-3. 운동 탭 — 운동 목록 화면 (`ExerciseListScreen`)

**역할**: 선택한 부위별로 운동 종목과 세트/무게를 입력하는 화면

**UI 요소**:

| 영역 | 요소 | 설명 |
|---|---|---|
| 헤더 | `←` 뒤로가기 + "운동 목록" 타이틀 | `←` 탭 → 부위 선택 화면으로 복귀 |
| 부위 탭바 | 선택된 부위 수평 스크롤 탭 | 활성 탭: 라임 배경 / 비활성: 어두운 배경 |
| 운동 카드 목록 | 활성 탭 부위의 운동 목록 | |
| 운동 카드 > 이름 입력 | `text input` placeholder "운동 이름" | 자유 텍스트 입력 |
| 운동 카드 > 삭제 버튼 | `✕` 버튼 | 탭 → 해당 운동 즉시 삭제 |
| 운동 카드 > 수치 입력 3열 | 무게(kg) / 세트 / 횟수 각각 `number input` | focus 시 라임 링 |
| 빈 상태 | "+ 운동을 추가하세요" 안내 텍스트 | 해당 부위 운동 없을 때 |
| 운동 추가 버튼 | 점선 테두리 `+ 운동 추가` 버튼 | 탭 시 기본값(세트3, 횟수10)으로 카드 추가 |
| 운동 시작 버튼 | `w-full h-14` 라임 "운동 시작" 하단 고정 | 탭 → 타이머 화면으로 이동 |

**사용자 행동**:
- 부위 탭 탭 → 해당 부위 운동 목록으로 전환
- "+ 운동 추가" 탭 → 현재 탭 부위에 빈 운동 카드 추가
- 이름/무게/세트/횟수 입력 → 실시간 상태 업데이트
- `✕` 탭 → 해당 운동 카드 즉시 제거
- "운동 시작" 탭 → 타이머 시작 + 타이머 화면 이동 (탭 전환 잠금)

---

### 3-4. 운동 탭 — 타이머 화면 (`TimerScreen`)

**역할**: 운동 중 경과 시간 측정 및 운동/휴식 전환 기록

> 이 화면에서는 하단 탭 전환이 잠김 (비활성 탭 40% 투명도로 표시)

**UI 요소**:

| 영역 | 요소 | 설명 |
|---|---|---|
| 상태 뱃지 | "운동 중" (라임) / "휴식 중" (앰버) + 펄스 점 | 현재 상태 표시 |
| 타이머 숫자 | 중앙 대형 `HH:MM:SS` — 최대 80px | 운동 중: 라임 / 휴식 중: 앰버 |
| 랩 로그 | 고정 높이(h-44) 스크롤 가능한 이벤트 목록 | 최신 항목으로 자동 스크롤 |
| 랩 로그 행 | 타임스탬프 + 이벤트명 + 이전 구간 소요시간 | 이벤트: 운동 시작 / 휴식 시작 / 운동 재개 / 운동 완료 |
| 휴식 버튼 | `w-full h-14` 회색 "휴식" | 운동 중 상태일 때만 표시 |
| 운동 재개 버튼 | `w-full h-14` 앰버 "운동 재개" | 휴식 중 상태일 때만 표시 |
| 운동 완료 버튼 | `w-full h-14` 라임 "운동 완료" | 항상 표시 |

**사용자 행동**:
- "휴식" 탭 → 상태: resting + 앰버로 전환 + 랩 로그에 "휴식 시작" 기록
- "운동 재개" 탭 → 상태: working + 라임으로 전환 + 랩 로그에 "운동 재개" 기록
- "운동 완료" 탭 → 랩 로그 마감 + 통계 계산 + localStorage 저장 → 요약 화면 이동 (탭 전환 잠금 해제)

---

### 3-5. 운동 탭 — 요약 화면 (`SummaryScreen`)

**역할**: 방금 완료한 운동 세션의 결과를 요약해 보여주는 화면

**UI 요소**:

| 영역 | 요소 | 설명 |
|---|---|---|
| 헤더 | 날짜(단축) + "운동 완료" 타이틀 + 부위 pill 뱃지들 | |
| 통계 그리드 | 2열 4개 카드 | 총 운동 시간 / 휴식 전환 횟수 / 평균 운동 구간 / 평균 휴식 구간 |
| 운동 내역 | 부위별 섹션 (라임 소제목) + 운동 카드 목록 | |
| 운동 카드 | 운동 이름 + "세트×횟수" + 무게 pill | |
| 새 운동 시작 버튼 | `w-full h-14` 라임 | 탭 → 모든 상태 초기화 + 부위 선택 화면으로 |
| 기록 보기 버튼 | `w-full h-14` 어두운 회색 | 탭 → 운동 기록 화면으로 이동 |

**사용자 행동**:
- "새 운동 시작" 탭 → 운동 플로우 전체 초기화 → 부위 선택 화면
- "기록 보기" 탭 → 운동 기록 화면으로 이동 (뒤로가기 시 이 요약 화면으로 복귀)

---

### 3-6. 운동 탭 — 운동 기록 화면 (`HistoryScreen`)

**역할**: 전체 운동 기록을 시간순(최신순)으로 목록 확인 및 삭제

**UI 요소**:

| 영역 | 요소 | 설명 |
|---|---|---|
| 헤더 | `←` 뒤로가기 + "운동 기록" 타이틀 + 기록 수 (`N개`) | |
| 세션 카드 목록 | 날짜 + 부위 + 총 시간 pill + 삭제 버튼 | |
| 세션 카드 > 운동 태그 | 각 운동의 "이름 Nkg N×N" 태그 행 | |
| 세션 카드 > 간단 통계 | 휴식 N회 / 평균 운동 / 평균 휴식 | |
| 삭제 플로우 | 쓰레기통 아이콘 탭 → "취소" + "삭제" 버튼으로 교체 | 2단계 확인 |
| 빈 상태 | "아직 운동 기록이 없어요" + "첫 운동을 시작해보세요!" | |

**사용자 행동**:
- `←` 탭 → 이전 화면 (요약 화면에서 왔으면 요약으로, 아니면 부위 선택으로)
- 쓰레기통 탭 → 삭제 확인 UI 표시
- "취소" 탭 → 확인 UI 해제
- "삭제" 탭 → 해당 세션 삭제 + 리스트 갱신

---

### 3-7. 식단 탭 (`DietTab`)

**역할**: 식단 기록 (미구현)

**UI 요소**: 머그컵 아이콘 + "식단 기록" 타이틀 + "준비 중인 기능이에요 / 곧 업데이트될 예정입니다" 안내 텍스트 (화면 중앙 정렬)

---

## 4. 화면 간 이동 플로우

```
[앱 진입]
    │
    ├── 홈 탭 (HomeTab)
    │       └── 캘린더 날짜 탭 → 인라인 상세 펼침 (화면 이동 없음)
    │
    ├── 운동 탭 (workout flow)
    │       │
    │       ▼
    │   [부위 선택 화면] ──(부위 선택 후 "다음 →")──▶ [운동 목록 화면]
    │         ▲                                              │
    │         └──────────── "←" 뒤로가기 ──────────────────┘
    │                                                        │
    │                                           "운동 시작" 탭
    │                                                        │
    │                                                        ▼
    │                                              [타이머 화면]
    │                                    (탭 전환 잠금 ON)  │
    │                                                        │
    │                                           "운동 완료" 탭
    │                                    (탭 전환 잠금 OFF) │
    │                                                        ▼
    │                                              [요약 화면]
    │                                          ┌──────┴──────┐
    │                              "새 운동 시작"          "기록 보기"
    │                                  │                       │
    │                                  ▼                       ▼
    │                          [부위 선택 화면]        [운동 기록 화면]
    │                                                          │
    │                                                       "←" 탭
    │                                                          │
    │                                              요약에서 왔으면 → [요약]
    │                                              아니면 → [부위 선택]
    │
    └── 식단 탭 (DietTab) — 플레이스홀더
```

---

## 5. 공통 디자인 토큰

| 역할 | 값 |
|---|---|
| 배경 | `bg-zinc-950` |
| 카드 배경 | `bg-zinc-900` |
| 카드 테두리 | `border-zinc-800/60` |
| 포인트 컬러 | `lime-400` (`#a3e635`) |
| 휴식 컬러 | `amber-400` |
| 위험(삭제) 컬러 | `red-400` |
| 기본 텍스트 | `text-zinc-50` |
| 보조 텍스트 | `text-zinc-400` ~ `text-zinc-600` |
| 버튼 모서리 | `rounded-2xl` |
| 주요 버튼 | `w-full h-14 rounded-2xl` |
| 폰트 — 레이블 | `font-sans` |
| 폰트 — 숫자/시간 | `font-mono` |
| 탭 전환 애니메이션 | `animate-fade-in` (탭 또는 화면 전환 시) |

---

## 6. 데이터 모델 요약

```ts
WorkoutSession {
  id: string
  date: string          // "YYYY-MM-DD"
  bodyParts: string[]   // 선택한 부위
  exercises: Exercise[] // 운동 목록
  lapLog: LapEntry[]    // 타이머 이벤트 로그
  totalDuration: number // 초
  avgWorkTime: number   // 초
  avgRestTime: number   // 초
}

Exercise {
  id, bodyPart, name,
  weight: number,  // kg
  sets: number,
  repsPerSet: number
}

LapEntry {
  timestamp: number  // 초 (운동 시작으로부터)
  type: 'workout_start' | 'rest_start' | 'workout_resume' | 'complete'
}
```

데이터 저장: `localStorage` key `"workout-history"` (JSON 배열, 최신순)
